# phase A (CPU prototype)
numpy>=1.26
scipy>=1.12
quimb>=1.7

# phase B (GPU hot path) — uncomment when CUDA 12.8+ is ready
# cupy-cuda12x>=13.0
# cuquantum-python-cu12>=24.03

# dev / testing
pytest>=8.0
matplotlib>=3.8   # optional: for plotting benchmark results
